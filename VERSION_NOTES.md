# 版本说明

本文档记录每次固件和资源版本的主要修改内容。固件版本对应根目录 `CMakeLists.txt` 中的 `PROJECT_VER`，表情资源版本对应 `ASSETS_VER`。

## 2.2.22

Assets 版本：`1.0.12`

### 新增与调整
- 主固件版本号更新为 `2.2.22`。
- 表情资源版本号更新为 `1.0.12`。
- 音乐播放测试流改为直接使用 `/stream?file=1.mp3`，减少转码服务内部 HTTP 嵌套。
- 网络收音机播放流请求超时时间调整为 20 秒，避免转码首包较慢时误报响应异常。

## 2.2.21

Assets 版本：`1.0.11`

### 新增与调整
- 主固件版本号更新为 `2.2.21`。
- 表情资源版本号更新为 `1.0.11`。
- 修复 `radio_stream` 播放音乐代理流时的任务栈溢出问题。
- 优化音乐代理在客户端断开连接时的异常处理。

## 2.2.20

Assets 版本：`1.0.10`

### 新增与调整
- 主固件版本号更新为 `2.2.20`。
- 表情资源版本号更新为 `1.0.10`。
- 新增 `self.music.play_test` MCP 测试工具，用于播放音乐转码代理测试流。

## 2.2.19

Assets 版本：`1.0.9`

### 新增与调整

- 主固件版本号更新为 `2.2.19`。
- 表情资源版本号更新为 `1.0.9`。
- WiFi/4G 状态栏图标改为白色，适配黑色状态栏背景。
- WiFi 图标按 30x30 顶部状态栏槽位重新调整尺寸，避免显示过大。
- 4G 图标按 30x30 顶部状态栏槽位重新调整尺寸，使用白色竖条显示信号强度。

## 2.2.18

Assets 版本：`1.0.8`

### 新增与调整

- 主固件版本号更新为 `2.2.18`。
- 表情资源版本号更新为 `1.0.8`。
- 按图示重新制作 WiFi/4G 状态栏图标资源。
- WiFi 使用扇形强度图标，离线状态带斜杠。
- 4G 使用竖条强度图标，离线状态带叉号，和 WiFi 图标明确区分。

## 2.2.17

Assets 版本：`1.0.7`

### 新增与调整

- 主固件版本号更新为 `2.2.17`。
- 表情资源版本号更新为 `1.0.7`。
- 4G 状态图标改为竖条信号强度样式，弱/中/良好/强分别显示不同数量的黑色竖条，和 WiFi 扇形图标区分。
- XiaoZhiWave 默认网络改为 WiFi。
- 如果误切到 4G 且 ML307 模组不可用，会自动保存回 WiFi 并重启，避免开机卡在启动画面。

## 2.2.16

Assets 版本：`1.0.6`

### 新增与调整

- 主固件版本号更新为 `2.2.16`。
- 表情资源版本号更新为 `1.0.6`。
- 为 XiaoZhiWave emote assets 增加 WiFi/4G 信号图标资源入口，打包时会进入 `expression_assets.bin`。
- 状态栏按 WiFi/4G 类型和信号强弱选择独立图标资源名，并继续显示 `WiFi`/`4G` 标签。
- 4G 状态图标改为竖条信号强度样式，弱/中/良好/强分别显示不同数量的黑色竖条，和 WiFi 扇形图标区分。
- 优化开机联网阶段显示：搜索网络、联网中、同步配置、检查更新、准备语音服务都会在屏幕上给出阶段反馈。
- 新增网络收音机 MCP/语音工具：`self.radio.play` 和 `self.radio.stop`。
- 网络收音机支持在没有 URL 时按电台名称查询公开电台目录，并优先选择 Ogg Opus 可直连流。
- 新增 `self.radio.list`，支持语音询问“有哪些电台可以播放”，返回当前固件可直接播放的 Ogg Opus 候选电台。
- “播放网络收音机”未指定电台名时，会随机选择一个可直连 Ogg Opus 电台，方便测试播放功能。
- XiaoZhiWave 默认网络改为 WiFi；如果误切到 4G 且 ML307 模组不可用，会自动保存回 WiFi 并重启，避免开机卡在启动画面。

### 说明

- 网络收音机第一版支持直连 Ogg Opus 网络流 URL。常见 MP3/AAC 电台需要额外解码器或后台转码后再接入。
- 4G 竖条图标为 30x30 `RGB565A8` emote 图标资源，后续可以直接替换对应 `.bin` 文件微调视觉样式。

## 2.2.15

Assets 版本：`1.0.5`

### 新增与调整

- 主固件版本号更新为 `2.2.15`。
- 表情资源版本号更新为 `1.0.5`。
- emote 状态栏电量显示移动到右上角，避免和信号图标重合。
- 电量显示改为百分比文字，不判断充电状态。
- emote 状态栏增加网络类型标签，区分 `WiFi` 和 `4G`。
- 语音倒计时结束提示音从 3 次增加为连续 9 次。

## 2.2.15 后续调整

Assets 版本：`1.0.5`

### 新增与调整

- 为 XiaoZhiWave emote assets 增加 WiFi/4G 信号图标资源入口，打包时会进入 `expression_assets.bin`。
- 状态栏按 WiFi/4G 类型和信号强弱选择独立图标资源名，并继续显示 `WiFi`/`4G` 标签。
- 优化开机联网阶段显示：搜索网络、联网中、同步配置、检查更新、准备语音服务都会在屏幕上给出阶段反馈。
- 新增网络收音机 MCP/语音工具：`self.radio.play` 和 `self.radio.stop`。
- 网络收音机支持在没有 URL 时按电台名称查询公开电台目录，并优先选择 Ogg Opus 可直连流。

### 说明

- 网络收音机第一版支持直连 Ogg Opus 网络流 URL。常见 MP3/AAC 电台需要额外解码器或后台转码后再接入。
- 当前新增的 WiFi/4G 图标使用 emote 同格式资源占位，后续可以直接替换对应 `.bin` 文件获得更精细的强弱图标。

## 2.2.13

Assets 版本：`1.0.4`

### 新增与调整

- 主固件版本号更新为 `2.2.13`。
- 保持表情资源版本号 `1.0.4` 不变。
- 去除屏幕朝下触发 10 秒倒计时的逻辑。
- 屏幕朝下保留免打扰/静音状态，不再启动倒计时。
- emote 状态栏增加电量百分比显示，不判断充电状态。
- emote 状态栏增加网络状态图标显示，使用现有资源显示有信号/无信号状态。
- 语音倒计时结束时增加连续提醒音，避免只显示文字提醒。

## 2.2.12

Assets 版本：`1.0.4`

### 新增功能

- 为 XiaoZhiWave ESP32-S3 Touch LCD 2 板卡接入 QMI8658 姿态感知。
- 支持“拿起来”感知：设备从睡眠表情恢复为睁眼/活跃状态。
- 支持“向左倾”感知：表情产生向左偏移，增强设备被拿着时的反馈感。
- 支持“快速摇晃”感知：显示晕了/惊讶类表情，并提示“别摇啦”。
- 支持“平放不动”感知：进入休眠表情。
- 支持“屏幕朝下”感知：自动静音，并显示免打扰状态。
- 支持“再翻回来”恢复：退出免打扰/静音状态，但不会擅自开始录音。
- 接入 emote 大眼睛表情资源，表情表现比原普通表情更生动。
- 新增 emote 模式下的触摸唤醒任务，解决没有 LVGL display 时触摸无法唤醒的问题。
- 新增语音定时器功能：可通过语音/MCP 启动倒计时，到时间后语音提醒并返回聊天界面。
- OTA 支持 assets 分区独立升级，固件 `xiaozhi.bin` 和表情资源 `expression_assets.bin` 可按各自版本分别升级。

### 修复与调整

- 修复 emote assets 打包时误使用组件默认 `你好喵伴` WakeNet 模型的问题。
- 唤醒词恢复为 menuconfig 选择的 `Hi,喵喵` / `喵喵同学`。
- 修复唤醒词文本和后台人设回复不一致的问题。
- 保留竖屏显示方案，未启用横屏/自动旋转方案。
- 更新新分区表下的合并固件资源文件名为 `expression_assets.bin`，assets 分区地址为 `0xC00000`。

### OTA 说明

- 固件版本号：`PROJECT_VER = 2.2.12`
- 表情资源版本号：`ASSETS_VER = 1.0.4`
- 固件 OTA 文件：`xiaozhi.bin`
- 表情资源 OTA 文件：`expression_assets.bin`
- 合并烧录时 assets 分区使用：`0xC00000 build\expression_assets.bin`
